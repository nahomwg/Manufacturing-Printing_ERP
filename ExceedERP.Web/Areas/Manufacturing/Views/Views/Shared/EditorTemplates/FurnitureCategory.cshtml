
@(Html.Kendo().DropDownListFor(m => m)
        .OptionLabel("-- Select --")
        .DataTextField("Name")
        .DataValueField("FurnitureCategoryId")
        //.Events(ev =>
        //{
        //    ev.Change("selectInventory").DataBound("selectInventory");

        //})
        .DataSource(dataSource =>
        {
            dataSource.Read(read => read.Action("GetFurnitureCategory", "PrintingEstimationDataProvider"))
            .ServerFiltering(false);

        })
        .Filter("contains")
        .MinLength(2)
        .ValuePrimitive(true)
)
