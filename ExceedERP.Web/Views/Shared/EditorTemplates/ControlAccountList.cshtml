@(Html.Kendo().DropDownListFor(m => m)
                  .AutoBind(false)
                  .OptionLabel("--Select Control Account--")
                  .DataTextField("Description")
                  .DataValueField("ControlAccountID")
                  .DataSource(dataSource =>
                  {
                      dataSource.Read(read => read.Action("GetControlAccountList", "DataProvider"))
                                .ServerFiltering(true);
                  })

)
@Html.ValidationMessageFor(m => m)