@using System.Collections
@using ExceedERP.Core.Domain.HRM
@(Html.Kendo().DropDownListFor(m=> m).BindTo((IEnumerable)ViewData["RankList"])
.OptionLabel("Select..")
                    .DataTextField("RankName")
                    .DataValueField("RankId")
                         .ValuePrimitive(true)
                                 
)