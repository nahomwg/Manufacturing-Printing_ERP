@(Html.Kendo().DropDownListFor(m => m)
                  .AutoBind(false)
                  .OptionLabel("--Select Sub Country--")
                  .DataTextField("Name")
                  .DataValueField("SubCountryID")
                  .DataSource(dataSource =>
                  {
                      dataSource.Read(read => read.Action("GetSubCountry", "DataProvider"))
                                .ServerFiltering(true);
                  })

)
@Html.ValidationMessageFor(m => m)