@(Html.Kendo().DropDownListFor(m => m)
                  .AutoBind(false)
                  .OptionLabel("--Select Holiday Definition--")
                  .DataTextField("Description")
                  .DataValueField("HolidayDefinitionID")
                  .DataSource(dataSource =>
                  {
                      dataSource.Read(read => read.Action("GetHolidayDefinition", "DataProvider"))
                                .ServerFiltering(true);
                  })

)
@Html.ValidationMessageFor(m => m)
